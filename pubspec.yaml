name: dental_app
description: "DR MALEK ALROMIMAH DENTAL - real web, Gemini, news and dental library"
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_storage: ^12.3.4
  cloud_firestore: ^5.4.4
  google_generative_ai: ^0.4.6
  video_player: ^2.9.2
  chewie: ^1.8.5
  flutter_pdfview: ^1.3.2
  dio: ^5.7.0
  path_provider: ^2.1.4
  open_filex: ^4.5.0
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
