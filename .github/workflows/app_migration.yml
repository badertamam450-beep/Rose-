name: Install DR MALEK ALROMIMAH DENTAL UI

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check migrated interface
        run: test -f lib/main.dart && grep -q "DR MALEK ALROMIMAH DENTAL" lib/main.dart
